{
  "name": "catalogue-search-demo",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "preview:cloudflare": "npm run build && wrangler dev",
    "deploy": "npm run build && wrangler deploy",
    "test": "vitest run"
  },
  "devDependencies": {
    "vite": "^7.1.3",
    "vitest": "^3.2.4",
    "wrangler": "4.119.0"
  },
  "overrides": {
    "undici": "7.29.0"
  }
}
